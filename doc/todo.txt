In progress

Next
 - type-safe records
   - verify field names and type on record set
 - records
   - record=

oo.txt

Unit tests
 - cond -- (setl x 5)(cond ((not(num? x)) 'notanum) ((< x 0) -1) ((> x 0) +1) (true 0))
 - record=
 - eq on records
 - record functions
   - get-type-name
     - incl on primitives and records
   - is-a
   - type-of
   - type-exists
   - get-type

Lisp lib
 - data types
   - dict type -- for / from oo
 - fold
 - variadic and, or.
 - let
 - pp

JS lib
 - data types
   - LispExpressions require memory allocation to traverse!
     - Profile to confirm speed issue then perhaps switch to real linked lists.
   - Arrays
   - Hashes?
 - split tests into separate files
 - macro-expand -- no eval!

Lang
 - quasiquoting
 - load external files - and split up prologue
 - comments
 - strings

Perf profiling. Unit tests take 20 secs wtf?

Future ideas
 - deconstructing value bind
 - debugging -- eval once
 - pure functions
   - bind to values, not scopes?
 - pre-evaluate macros for a function definition
 - static typing
 - compile to JS

Code puzzles
 - primality tests.
 - Fibonacci with memoization.
 - balanced tree data structures.
 - parser.
