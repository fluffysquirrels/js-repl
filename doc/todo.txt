In progress
 - ListDict.new as macro
 - fix ListDict.from-list
 - macrotest
 - geometry.lisp incl vec2 as trial for type-safe records.
 - dict=

 - type-safe records
   - verify field names and type on record set
   - remove 2nd field value (type name)
 - records
   - record=

oo.txt

Unit tests
 - ListDict.new, ListDict.from-list
 - macrotest
 - record=
 - eq on records
 - add-type
 - parse floats
 - vec2
 - sqrt
 - square

Lisp lib
 - macrotest -- creates a new macro that returns the list that would be executed if this definition was created as a macro. e.g. ( (macrotest (expr) (list 'setl 'my-expr expr)) (* x y)) returns '(setl my-expr (* x y))
 - fold
 - variadic and, or.
 - pp
 - switch

JS lib
 - data types
   - Arrays
   - Hashes
   - Date
   - Strings
 - split tests into separate files

Lang
 - quasiquoting
 - better parser and tokeniser
 - comments
 - strings
 - optional args

Code puzzles
 - primality tests.
 - Fibonacci with memoization.
 - balanced tree data structures.
 - parser.
 - regex.

Future ideas
 - mutable private data; start with the types list.
 - load external files
 - deconstructing value bind
 - debugging -- eval once
 - pure functions
   - bind to values, not scopes?
 - pre-evaluate macros for a function definition
 - static typing
 - compile to JS
 - exception handling / conditions
 - tail call elimination
 - ffi to JavaScript
 - macro-expand -- no eval!

